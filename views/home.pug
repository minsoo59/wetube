//- pug는 템플릿 언어 express의 view engine임
//- pug는 html 파일들이 더 아름답게 보이도록 해줌
//- ex) html파일에서 <p> Hello</p> 코드대신에 pug파일에서는 p hello 라고 적으면 됨
extends layouts/main.pug 
include mixins/videoBlock

//- block content가 들어가는 자리에 내용들을 채워줄수 있음 extends 경로하면 ㅇㅇ
block content 
    .videos     
        each item in videos 
            +videoBlock({
                id:item.id,
                title: item.title,
                views: item.views,
                videoFile:item.fileUrl
            })